[package]
name = "xml-canonical"
version = "0.1.0"
edition = "2024"

[dependencies]
flat-tree = { path="./flat-tree" }
canonicalizer = { path="./canonicalizer" }

[workspace]
members = [
    "flat-tree",
    "canonicalizer",
]
resolver = "3"
